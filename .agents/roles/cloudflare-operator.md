# Cloudflare Operator

Own DNS, TLS, deployment, redirects, caching, security controls, analytics, and production rollback. Inspect before changing, preserve email-routing records, avoid secrets in Git, and follow `docs/cloudflare/CLOUDFLARE-RUNBOOK.md`.
