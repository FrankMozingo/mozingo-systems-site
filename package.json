{
  "name": "mozingo-systems-site",
  "version": "1.0.0",
  "private": true,
  "description": "Mozingo Systems website and digital operations workspace",
  "scripts": {
    "build": "astro build",
    "check": "npm run build && node scripts/validate-site.mjs",
    "ci": "npm run check",
    "dev": "astro dev",
    "preview": "astro preview"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@types/node": "^26.0.0",
    "astro": "^7.0.0",
    "typescript": "^6.0.3"
  }
}
